# indicator file for API changes.
swaggerFileName : ["{{ file_name }}", "{{ variable_name }}"]
# default names of packages to bump.
defaultPackages : ["{{ package_name }}"]
# paths to files where to bump API version with corresponding variables.
apiPathsWithVars : [["{{ file_name }}", "{{ variable_name }}"]]
